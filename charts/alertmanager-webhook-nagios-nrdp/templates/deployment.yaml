apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "alertmanager-webhook-nagios-nrdp.fullname" . }}
  labels:
    {{- include "alertmanager-webhook-nagios-nrdp.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "alertmanager-webhook-nagios-nrdp.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "alertmanager-webhook-nagios-nrdp.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "alertmanager-webhook-nagios-nrdp.serviceAccountName" . }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: alertmanager-webhook-nagios-nrdp
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default (printf "v%s" .Chart.AppVersion) }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args:
            - serve
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
              {{- if .Values.tls.server.enabled }}
              scheme: HTTPS
              {{- end }}
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
              {{- if .Values.tls.server.enabled }}
              scheme: HTTPS
              {{- end }}
          {{- if or .Values.nrdpToken.existingSecret .Values.webhookAuth.existingSecret .Values.extraEnv }}
          env:
            {{- if .Values.nrdpToken.existingSecret }}
            - name: NRDP_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.nrdpToken.existingSecret }}
                  key: {{ .Values.nrdpToken.existingSecretKey }}
            {{- end }}
            {{- if .Values.webhookAuth.existingSecret }}
            - name: WEBHOOK_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.webhookAuth.existingSecret }}
                  key: {{ .Values.webhookAuth.passwordKey }}
                  optional: true
            - name: WEBHOOK_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.webhookAuth.existingSecret }}
                  key: {{ .Values.webhookAuth.tokenKey }}
                  optional: true
            {{- end }}
            {{- with .Values.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          {{- end }}
          volumeMounts:
            - name: config
              mountPath: /etc/nrdp-webhook
              readOnly: true
            {{- if .Values.tls.server.enabled }}
            - name: server-tls
              mountPath: /etc/nrdp-webhook/tls
              readOnly: true
            {{- end }}
            {{- if .Values.tls.nrdp.enabled }}
            - name: nrdp-tls
              mountPath: /etc/nrdp-webhook/nrdp-tls
              readOnly: true
            {{- end }}
            {{- with .Values.extraVolumeMounts }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      volumes:
        - name: config
          configMap:
            name: {{ include "alertmanager-webhook-nagios-nrdp.fullname" . }}
        {{- if .Values.tls.server.enabled }}
        - name: server-tls
          secret:
            secretName: {{ required "tls.server.existingSecret is required when tls.server.enabled is true" .Values.tls.server.existingSecret }}
        {{- end }}
        {{- if .Values.tls.nrdp.enabled }}
        - name: nrdp-tls
          secret:
            secretName: {{ required "tls.nrdp.existingSecret is required when tls.nrdp.enabled is true" .Values.tls.nrdp.existingSecret }}
        {{- end }}
        {{- with .Values.extraVolumes }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
